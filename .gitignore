# dependencies
/node_modules
/package-lock.json

# optional npm cache directory
.npm

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# build output (if you use TypeScript or build scripts in the future)
dist
build

# environment files (if ever needed)
.env
